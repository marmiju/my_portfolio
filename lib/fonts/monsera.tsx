import { Montserrat_Alternates, Pixelify_Sans } from "next/font/google";

export const montserat = Montserrat_Alternates({
  subsets: ["latin"],
  weight: "600",
});
export const pixelfy = Pixelify_Sans({
  subsets: ["cyrillic"],
});
export const monsterat_text = Montserrat_Alternates({
  subsets: ["latin"],
  weight: "500",
});
